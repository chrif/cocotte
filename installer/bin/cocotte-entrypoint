#!/bin/sh

set -eu

php /installer/bin/bootstrap-container

if 	[ "$1" = "install" ] || [ "$1" = "wizard" ] || [ "$1" = "uninstall" ] || \
	[ "$1" = "static-site" ] || [ "$1" = "help" ]; then
	exec console "$@"
else
	exec "$@"
fi
