version: "3"

services:

  installer:
    build: installer
    image: "chrif/cocotte:${INSTALLER_VERSION:-latest}"

  travis:
    build: docker/travis
    volumes:
      - .:/opt/project
    environment:
      TRAVIS_TOKEN: ${TRAVIS_TOKEN}
